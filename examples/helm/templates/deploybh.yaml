# Copyright 2023 Specter Ops, Inc.
#
# Licensed under the Apache License, Version 2.0
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#     http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.
#
# SPDX-License-Identifier: Apache-2.0

apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.apihound.name }}
  namespace: {{ .Values.namespace }}
spec:
  replicas: {{ .Values.apihound.replicas }}
  selector:
    matchLabels:
      app: {{ .Values.apihound.appName }}
  template:
    metadata:
      labels:
        app: {{ .Values.apihound.appName }}
    spec:
      initContainers:
      - name: check-db-availability
        image: busybox
        command: ['/bin/sh', '-c']
        args:
        - >
          until nc -z {{ .Values.graphdb.service.name }} {{ (index .Values.graphdb.service.ports 0).port }} && nc -z {{ .Values.appdb.service.name }} {{ .Values.appdb.service.port }}; do
            echo "Waiting for databases to be ready";
            sleep 5;
          done;
      containers:
      - name: {{ .Values.apihound.container.name }}
        image: {{ .Values.apihound.container.image }}:{{ .Values.apihound.container.version }}
        env:
        - name: bhe_disable_cypher_complexity_limit
          value: "{{ .Values.apihound.container.env.bhe_disable_cypher_complexity_limit }}"
        - name: bhe_enable_cypher_mutations
          value: "{{ .Values.apihound.container.env.bhe_enable_cypher_mutations }}"
        - name: bhe_graph_query_memory_limit
          value: "{{ .Values.apihound.container.env.bhe_graph_query_memory_limit}}"
        ports:
        - containerPort: {{ (index .Values.apihound.container.ports 0).containerPort }}
        {{- if .Values.apihound.configMap.enabled }}
        volumeMounts:
        - name: apihound-config
          mountPath: /apihound.config.json
          subPath: apihound.config.json
        {{- end }}
      {{- if .Values.apihound.configMap.enabled }}
      volumes:
      - name: apihound-config
        configMap:
          name: {{ .Values.apihound.configMap.name }}
      {{- end }}
